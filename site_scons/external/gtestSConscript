Import ('env')

env.Replace(CXXFLAGS=[], CFLAGS=[])
name = 'gtest'
inc = env.Dir('.'), env.Dir('include'), env.Dir('include/gtest')
ext_inc = env.Dir('include/gtest')
src = ['src/gtest-all.cc']
deps = []

env.CreateStaticLibrary(name, inc, ext_inc, src, deps)
env.CreateDoc(name)
